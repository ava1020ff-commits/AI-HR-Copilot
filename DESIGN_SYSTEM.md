# AI HR Workbench Design System v1.0

企业级 AI + HR 招聘工作台。专业、简洁、克制、现代、可信赖、数据驱动。

## 唯一来源与使用方式

- `services/design_tokens.css` 是设计变量的唯一来源；`services/theme.css` 仅使用变量定义公共组件。
- 所有页面调用 `apply_saas_theme()`；页面禁止注入独立 CSS、硬编码颜色、字号、圆角或阴影。
- 标题、空状态、标签、AI 说明使用 `services/ui.py`，首页业务组件使用 `services/workspace_ui.py`。
- 图表调用 `services.design_system.apply_chart_theme()`，颜色通过 `token()` 获取。
- iframe 无法继承父页面样式，必须从 `services.design_system` 引入共享样式与 Tokens。
- `.streamlit/config.toml` 的原生主题颜色与 Tokens 保持一致，由自动化测试检查，避免原生表格和页面主题分裂。
- 新需求先复用公共组件；确需扩展，修改公共 Tokens/组件与本规范，不在单个页面补丁式覆盖。

## 布局与基础规范

| 项目 | 统一值 / Token | 使用规则 |
| --- | --- | --- |
| 1. 页面最大宽度 | `content-max` 1400px | 包含页面内边距，居中；侧栏独立占 240px；不拉满超宽屏 |
| 2. 左右边距 | `gutter` 32px；平板 24px；手机 16px | ≤1068px 用平板，≤640px 用手机 |
| 3. 模块间距 | `module-gap` 32px | 模块内部组间 24px；字段间 16px；标签到控件 8px；图标文字 4–8px |
| 4. 卡片内边距 | `card-padding` 24px | 紧凑卡片和手机 16px；不做卡片套卡片 |
| 5. 圆角 | `radius` 8px；`control-radius` 6px | 禁用胶囊大圆角、浮雕和重阴影，默认 `shadow: none` |
| 6. 字号 | 标题 28、模块 20、卡片 16、正文 14、辅助 13、微文案 12、指标 32px | 手机标题 24px；数字使用等宽数字；正文行高 1.5，标题 1.3 |
| 7. 字重 | 400 / 500 / 600 | 正文 400，按钮和表头 500，标题和指标 600；不用整段加粗 |
| 8. 主色 | `accent` #2563EB | 仅主操作、选中导航、焦点和主要数据系列；AI 也使用同一主色 |
| 9. 辅助色 | 文字 #64748B；成功 #15803D；警告 #B45309；错误 #B91C1C | 状态必须带文字，不只依赖颜色；不按模块随意分配彩色背景 |
| 10. 背景 | 画布 #F7F8FA、表面 #FFFFFF、次表面 #F1F5F9 | 卡片统一白底；AI 模块不另设紫色视觉体系 |
| 11. 边框 | 分隔 #E2E8F0；控件 #94A3B8 | 1px 实线；控件边界比装饰分隔线更清晰 |
| 12. Hover | 次操作背景 #F8FAFC；主操作 #1D4ED8；按下 #1E40AF | 120ms 颜色过渡，无缩放或浮起；键盘焦点 2px 蓝色描边，偏移 2px |

间距基准：4 / 8 / 12 / 16 / 24 / 32 / 48px。信息层级：页面标题与一个主操作 → 核心待办指标 → 主工作区 → 辅助日程/预警。宽屏主辅区域优先 2:1 或 3:2，避免所有模块等权重。手机按业务优先级单列，表格局部横向滚动。

## 组件规范

| 组件 | 规范 |
| --- | --- |
| 13. 按钮 | 高 44px，横向内边距 16px，字号 14/500，圆角 6px。页面或独立操作区一个蓝色主按钮；次按钮白底描边；低频动作文字按钮；危险操作红色并保留明确动作名称。禁用状态保留标签，无 hover 反馈。长文本允许换行，不裁切。 |
| 14. 输入框 | 最小高 44px，字号 14px，白底，6px 圆角；标签放上方，提示在下方。占位符不能代替可访问标签。焦点用统一蓝色轮廓；错误用错误文字和边框；必填在标签标注。多行输入随内容类型设置高度，不能把高度当页面留白。 |
| 15. 表格 | 使用原生 `st.dataframe` / `st.data_editor`，统一浅灰表头、14px 文本，默认行高 44px、隐藏无意义索引、宽度适应容器。文本左对齐，数值右对齐；时间和单位口径明确。行分隔线替代密集竖线；选中用浅蓝，hover 仅轻灰。Canvas 表格保留原生排序、选择与可访问行为，不用 CSS 伪造单元格；其色彩继承原生主题。 |
| 16. 数据卡片 | 白底、1px 分隔边框、8px 圆角；标题 13px 次文字、数值 32px/600、说明 13px。最多一排四张。数值用真实数据，空数据计数显示 0，不编造增长率；单位和统计范围必须清晰。异常才使用状态色，不给每张卡片安排不同颜色。无点击行为的卡片不显示 hover 浮起或指针。 |
| 17. 空状态 | 公共 `render_empty_state()`：中性单色图标、16px 标题、一句原因/下一步、最多一个操作。空库、无搜索结果、权限不足、读取失败分别说明。空态不展示模拟记录；错误不能伪装为 0。保持简洁，不用大插画、庆祝 emoji 或营销话术。 |
| 18. Loading | 沿用原生 `st.spinner("正在…")` 显示实际动作；已知进度才用 `st.progress`，未知进度不编造百分比。保留页面结构，反馈出现在触发区域；禁止全页遮罩和跳动缩放。耗时操作防重复提交；失败给出原因与重试入口。遵循系统减少动画偏好。 |

## 图标、导航与图表

- 侧栏分组、活动项蓝色、其余中性色；所有页面共用导航，不创建另一套顶部菜单。
- 使用公共组件提供的单色符号，配文字；不在标题、按钮和卡片堆叠 emoji。浏览器页签图标不承担页面信息层级。
- 图表默认单一蓝色；多系列依次使用蓝、灰、青、紫，并配图例。成功/警告/错误色只表达对应语义。
- 图表统一白底、弱网格线、14px 标签、16px 标题；保留零值、样本量和统计范围，不加装饰性阴影。
- 加载状态、空状态、成功状态、错误状态属于同一组件生命周期，应同时设计。

## 验收

运行 `python -m pytest -q` 和 `git diff --check`。设计契约测试检查 Tokens 引用完整、原生主题一致、页面无独立样式及全局主题入口。

视觉验收覆盖 1440px、1024px 和 390px：标题与主操作不裁切；侧栏选中可辨认；表格局部滚动；四类状态可读；键盘焦点可见。自动化页面测试只验证渲染与行为，不能替代真实浏览器视觉验收。


## 首页四层结构

页面标题与当前北京时间 / 岗位筛选 → 核心招聘指标（待处理候选人、今日新增、面试中、Offer中） → 需要我处理 → 招聘进展（岗位进展） → AI 助手（JD分析、简历匹配、面试题生成、BOSS沟通话术）。

待处理候选人取未筛选与HR确认/面试/Offer阶段候选人的并集，按人去重；今日新增按北京时间的确认保存日期。待办列表按人岗组合计数，指标按人数统计。同一候选人可在不同岗位处于不同阶段。

岗位筛选同步作用于三个数据区域；没有岗位关联记录的简历仅在全部岗位下显示。没有历史流转事件，不展示转化率；没有面试排期数据，待安排面试显示“未接入”和“—”，不把未知数量显示成真实零待办。数据源缺失与真实零待办不能混淆。需要我处理只表示当前工作台记录，不声称按负责人隔离。


## 产品化设计原则（v1.1）

以下是结合本项目的设计取舍，不复制参照产品的品牌、配色或具体页面布局。

| 参考 | 提炼的原则 | 项目落实 |
| --- | --- | --- |
| [Notion：工作区导航](https://www.notion.com/help/navigate-with-the-sidebar) | 稳定的组织结构与清晰层级 | 高频列表优先，解释与编辑按需展开；侧栏减少无意义缩进 |
| [Linear：工作流状态](https://linear.app/docs/configuring-workflows) | 状态对应明确工作含义 | 统一文字状态标签；待处理、暂无待办、未接入分别呈现 |
| [Slack：侧栏分组](https://slack.com/help/articles/212596808-Adjust-your-sidebar-preferences) | 将相关工作集中在稳定分组 | 招聘管理、AI工具、分析使用同一导航；每页有明确当前位置 |
| [Greenhouse：岗位管线](https://support.greenhouse.io/hc/en-us/articles/204636805-Current-pipeline-per-job) | 岗位、候选人和阶段是业务核心；当前与历史口径分开 | 候选人列表优先，岗位可检索；保留当前岗位进展，不编造历史转化率 |

### 页面与交互

- 首页保留四层结构：指标、需要我处理、招聘进展、AI助手。指标解释进入帮助提示，待办说明进入“统计口径与数据说明”。
- 候选人页先浏览人才库，再展开导入、解析、复核。数据保存后的结构化详情默认折叠；编辑内容和授权规则保持原样。
- 岗位页先搜索和比较岗位列表，再查看或编辑一个岗位；没有开关岗状态数据时只标注“已保存”。
- 真实计数为空显示0；数据尚未接入显示“— / 未接入”；读取错误显示错误，不能假装空库。三个状态使用不同文案。
- 一个操作区域只强调一个主动作；无交互的卡片不浮起。键盘焦点可见、禁用状态明确、耗时动作保留进度反馈。
- 手机指标默认2×2，不用四张高卡片占满数屏；正文和操作按任务顺序换行。表格局部滚动。

### 交付边界

这套规范改善使用体验，不代表项目已具备完整企业SaaS能力。正式交付仍需按实际需求验收账号与权限、数据隔离、备份恢复和审计；当前尚无面试排期、岗位开关状态、完整招聘流转历史和负责人归属模型。未实现的能力不得以装饰标签或虚构数字呈现。本轮不新增这些后端能力。
